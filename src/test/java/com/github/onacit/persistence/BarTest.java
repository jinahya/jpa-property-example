package com.github.onacit.persistence;

public class BarTest extends BaseTest<Bar> {

    // -----------------------------------------------------------------------------------------------------------------
    public BarTest() {
        super(Bar.class);
    }

    // -----------------------------------------------------------------------------------------------------------------
    @Override
    Bar entityInstance() {
        return super.entityInstance();
    }
}
