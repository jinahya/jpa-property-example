package com.github.onacit.persistence;

class OBarTest extends BaseEntityTest<OBar> {

    // -----------------------------------------------------------------------------------------------------------------
    OBarTest() {
        super(OBar.class);
    }

    // -----------------------------------------------------------------------------------------------------------------
    @Override
    OBar entityInstance() {
        return super.entityInstance();
    }
}
